#include "main.h"

/**
 * _abs -  a function that computes the absolute value of an integer
 *
 * @n: compiled variable
 *
 * Return: absolute value of integer
 */
int _abs(int r)
{
	if (r > 0)
	{
		return (r);
	}
	else
	{
	return (-r);
	}
	return (0);
}
